/*
  db.js has been commented out for a static-only frontend. The original
  implementation used sqlite3 or a JSON fallback. For the static rebuild
  we're moving persistence to browser storage (localStorage) implemented
  in `static-api.js`.

  -- db.js (commented out) --

*/
